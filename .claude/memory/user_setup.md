---
name: User dev environment
description: User's machine setup — WSL2, dotfiles bare repo, nvim with copilot.vim, synced to Windows and an Omarchy laptop
type: user
originSessionId: ed46d82f-dba0-4470-b2e2-8e0352d338ad
---
- Uses a bare git repo for dotfiles, managed via a `dotfiles` shell alias (not `git` directly)
- Machines: WSL2 (primary), Windows, Omarchy laptop — all sync the same dotfiles
- Neovim with lazy.nvim; copilot.vim (github/copilot.vim), keybindings in `~/.config/nvim/lua/plugins/copilot.lua`
- GitHub username: mindless-scribbles
