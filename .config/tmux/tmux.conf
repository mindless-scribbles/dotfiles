# tmux 3.1+ prefers ~/.config/tmux/tmux.conf over ~/.tmux.conf, and Omarchy
# upgrades drop their own default here (see tmux.conf.omarchy-*.bak).
# Keep ~/.tmux.conf as the single source of truth; this file just points at it.
source-file ~/.tmux.conf
